import { StyleSheet } from "react-native";
import { hp, wp } from "../../../services";

export const styles = StyleSheet.create({
    btnView: {
        width: wp(80),
        marginTop: hp(2)
    },
    inputView: {
        marginTop: hp(5)
    }
}) 